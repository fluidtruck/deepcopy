name: release-please

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  release:
    uses: fluidtruck-platform/github-workflows/.github/workflows/release-please.yml@v1
    secrets: inherit
